%h1 Add a shift

= form_tag shifts_path do

  = label :shift, :shiftstart, 'ShiftStart'
  = datetime_select :shift, :shiftstart

  = label :shift, :shiftend, 'ShiftEnd'
  = datetime_select :shift, :shiftend

  = label :shift, :id, 'ShiftID'
  = text_field :shift, :id

  = submit_tag 'Save'

